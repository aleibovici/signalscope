FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npx prisma generate

CMD ["npx", "tsx", "--tsconfig", "tsconfig.scripts.json", "scripts/run-harvest.ts"]
